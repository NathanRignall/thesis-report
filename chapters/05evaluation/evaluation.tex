\graphicspath{{chapters/05evaluation/graphs}}

\chapter{Evaluation}

%For any practical projects, you should almost certainly have some kind
%of evaluation, and it's often useful to separate this out into its own
%chapter.

The purpose of this chapter is to analyse the performance of the proposed LLM scheduling algorithm.
We specifically investigate, for a given GPU budget, whether a heterogenous cluster is faster (higher throughput) than a homogenous cluster.
The goal is to demonstrate that heterogenous clusters are still more cost efficient than homogenous ones in a MoE environment.

We also investigate the performance of the two different scheduling levels separately, to try understand if both are performing effectively.
For the \textit{inner loop} we execute several test cases with pre-defined island sizes on a constant workload, here we evaluate the effectiveness of the linear optimisation function.
We perform several more test cases on the \textit{outer loop}, investigating how different search parameters affect the scheduling result.

Finally we execute several real-world tests on a variety of production workload traces on several different inventories.
%include an exploration of these traces

% Highlights
\section{System Performance}

To answer the core research question, \textit{``do heterogenous clusters outperform homogeneous clusters in MoE environments?''}, we need to perform several experiments.
Given a constant hourly GPU budget, we craft three different inventories for testing and execute the complete schedule algorithm.

Given the Bayesian Optimisation process is non-deterministic (due to the random initialisation), the scheduling algorithm is executed five times over with the same parameters.
Then, we take the average of the best performances from each trial and also extract the deviation score of the inner loop from these best trials.
This allows us to demonstrate how well-matched the solutions is to the targeted workload.
We execute these experiments using the Azure LLM \autocite{stojkovicDynamoLLMDesigningLLM2024} datasets for both code and conversation, which are later explored in \autoref{chap:traces}.

The key results are presented in \autoref{tab:inventory-performance-grouped}, where the heterogenous cluster (a) is able to perform up to $1.4 \times$ faster than the homogenous cluster (c) for the same price (code), $1.2 \times$ (conversation).
We use the conservative pricing estimate that the \texttt{H20} is $1\$/\text{hour}$, \texttt{H800} is $2\$/\text{hour}$, \texttt{H200} is $4\$/\text{hour}$ and model our overall GPU budget is $512\ \$/\text{hour}$.
Shallowsim is specifically designed for DeepSeek, which is currently profiled for either china-specific or unreleased GPUs, therefore it is challenging to extract pricing information for all GPUs.

%Therefore, if we were able to have concrete pricing on GPUs such as the \texttt{DGX-B300}, the difference in performance between heterogenous and homogenous would likely be even greater.

\begin{table}[ht]
  \centering
  % The tabular environment now has 9 columns
  \begin{tabular}{@{} l l ccc cc cc @{}}
    \toprule
    % Headers for B columns have been removed
    \multirow{2}{*}{} & \multirow{2}{*}{Type} & \multicolumn{3}{c}{GPU Inventory} & \multicolumn{2}{c}{Code (Azure LLM)} & \multicolumn{2}{c}{Conversation (Azure LLM)} \\
    
    % Cmidrules are adjusted for the remaining groups
    \cmidrule(lr){3-5} \cmidrule(lr){6-7} \cmidrule(lr){8-9}
    
    % Sub-headers for B columns are removed
    & & H200 & H800 & H20 & $R_{total}$ & $D_{total}$ & $R_{total}$ & $D_{total}$ \\
    \midrule
    
    % Data for B columns is removed from each row
    (a) & Hetero & 64  & 64  & 128 & $176\pm20$ & $18\pm18$ & $\bm{138\pm16}$ & $5\pm9$\\
    (b) & Hetero & 32  & 128 & 128 & $\bm{199\pm21}$ & $22pm15$ & $126\pm12$ & $45.5\pm46$\\
    (c) & Homo  & 128 & 0 & 0 & $\bm{139\pm46}$ &  $25\pm22$ & $\bm{114\pm5}$ & $7\pm10$\\
    \bottomrule
  \end{tabular}
  \caption[GPU inventory configurations with resultant performance metrics on the Azure LLM dataset for code and conversation.]{GPU inventory configurations with resultant performance metrics on the Azure LLM dataset for code and conversation. \textbf{Experiment Parameters:} batch size = $16$, iterations = $15$, trials = $5$, min island size = $2$, skew range = $\pm 5.0$.}
  \label{tab:inventory-performance-grouped}
\end{table}

% inner loop by itself
\section{Inner Loop}
\label{chap:}

We test the \textit{inner loop} using four test cases (island layouts) shown in \autoref{tab:05-gpu-island}, this is to confirm that the \textit{inner loop} is able to precisely map workloads to a variety of island scenarios.
One scenario from both homogeneous and heterogeneous uses GPU islands of the same size, the other uses GPU islands of different sizes.
These islands are crafted to show the importance of selecting appropriate island sizes and potential value of heterogeneous clusters.

\begin{table}[ht]
  \centering
  \small
  \begin{tabular}{@{} l l l @{}}
    \toprule
    Island                            & GPU Model      & Sizes (Size × Count)              \\
    \midrule
    (a) Homogeneous Same Size         & DGX-B300       & 32 × 8                            \\
    (b) Homogeneous Different Sizes   & DGX-B300       & 32 × 2, 16 × 4, 8 × 8, 4 × 16     \\
    \midrule
    \multirow{4}{*}{(c) Heterogeneous Same Size}
                                      & DGX-B300       & 32 × 2                            \\
                                      & Rubin-NVL144   & 32 × 2                            \\
                                      & H800           & 32 × 2                            \\
                                      & H20            & 32 × 2                            \\
    \addlinespace
    \multirow{4}{*}{(d) Heterogeneous Different Sizes}
                                      & DGX-B300       & 32 × 1, 16 × 1, 8 × 1, 4 × 2      \\
                                      & Rubin-NVL144   & 32 × 1, 16 × 1, 8 × 1, 4 × 2      \\
                                      & H200           & 8 × 4, 4 × 8                      \\
                                      & H20            & 8 × 4, 4 × 8                      \\
    \bottomrule
  \end{tabular}
  \caption{GPU island test cases for \textit{inner loop} analysis, with two homogeneous scenarios and two heterogeneous scenarios.}
  \label{tab:05-gpu-island}
\end{table}

As the \textit{inner loop} solution generation is deterministic we only execute each test case once, unless stated otherwise.
This is because the linear solver will produce the exact same solution upon every execution, given the same input variables.

% Also include some methodology as to why certain input variables were selected.

\origclearpage
\subsection{Solver Results}
% TODO - Must also include all the input parameters the solver 

We first simultaneously select the optimum parallelism configuration and benchmark the provided GPU islands for both prefill and decode.
The resultant throughput values are displayed in \autoref{fig:05-inner-benchmark}, where we can see that every island class is able to contribute to prefill throughput, but not all islands are capable of decode.
This is due to the decode phase requiring more memory to execute, hence not all islands are compatible.
% TODO - Add explanation as to where the ranges are from.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{inner_benchmark_all_inventories.pdf}
    \caption[Prefill and decode benchmark results for test case] 
    {Prefill and decode benchmark results for test case, each colour represents a different island in the configuration.}
    \label{fig:05-inner-benchmark}
\end{figure}

Using the collected benchmark values, we populate the $b^{\mathrm{prefill}}_{i j}$ and $b^{\mathrm{decode}}_{i j}$ variables in the linear solver.
Then, using artificial workload as previously shown in \autoref{fig:04-sample-prefill-pdf}, we execute the solve function to find the optimum $x_{\mathrm{prefill},\,i j}$ and $x_{\mathrm{decode},\,i j}$ values.
The resultant assignment percentages are displayed in \autoref{fig:05-inner-assignment} for both the prefill and decode phase.

In the homogenous scenario, the assignment allocation percentages closely resemble the target workload.
In contrast, heterogenous assignment allocation percentages appear random.
This is to be expected, as it reflects the heterogeneous nature of island performance, where one island might be allocated few ranges due to its ``slow'' performance.
Also, for all scenarios, one large island is generally allocated for decode and many islands for prefill, this displays that prefill is the bottleneck for throughput.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{inner_assignment_all_inventories.pdf}
    \caption[Assignment values percentage (per range) for each island test case]
    {Assignment values percentage (per range) for each island test case, using artificial workload as described in \autoref{fig:04-sample-prefill-pdf}}
    \label{fig:05-inner-assignment}
\end{figure}

Collating the assignment percentages with throughout measurements (\autoref{fig:05-inner-assignment} and \autoref{fig:05-inner-benchmark}) we can plot the actual throughput per island, shown in \autoref{fig:05-inner-throughput}.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{inner_throughput_all_inventories.pdf}
    \caption[Throughput per range for each island test case]
    {Throughput per range for each every island test case, using artificial workload as described in \autoref{fig:04-sample-prefill-pdf}}
    \label{fig:05-inner-throughput}
\end{figure}

\origclearpage
\subsection{Evaluator Results}

Previous plots demonstrate the allocation results using direct outputs from the linear solver at the specified range resolution.
We need to evaluate the solutions using the complete expected input sequence length distribution $p(s \mid w)$ to highlight any differences in performance.
These results are displayed in \autoref{fig:05-inner-trace-throughput}, where \textit{evaluated} refers to results from the evaluator and \textit{model} refers to results directly from the solver.
We also include the workload \textit{trace} as a reference to what the target throughput distribution is.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{inner_trace_throughput}
    \caption[Throughput per sequence length for each island test case]
    {Throughput per sequence length for each island test case, using artificial workload as described in \autoref{fig:04-sample-prefill-pdf}. Such probability trace is overlaid and scaled to demonstrate solution divergence.}
    \label{fig:05-inner-trace-throughput}
\end{figure}


Displayed in \autoref{fig:05-inner-trace-throughput}, the linear optimisation precisely maps work from the input trace to the GPUs.
However, for all but experiment (d), the prefill and decode phase throughputs are miss-aligned (diverging).
This is because the islands provided do not have a `naturally' aligning solution, due to their shape and size it is impossible to perfectly allocate prefill and decode, given they cannot be mixed.
 We can expect the outer optimiser to solve this issue, as it will be able to perfectly allocate island sizes. 

The oscillation displayed between expected and model throughput is expected, this is because the model executes at a lower resolution.
Hence any throughput allocations will likely be over allocated for sequence lengths shorter than the midpoint, but under allocated for longer lengths.

\subsubsection{Evaluator Method}
The true throughput is calculated by first benchmarking each exact sequence length (whilst also selecting the best parallelism configuration), for each island using the same method as described in \autoref{chap:inner-loop}.
Then, the throughputs are weighted with the $p$ value of each sequence length $s$ along with the island percentage allocation $x$, of the range the sequence length resides within.

%\subsection{Summary}
%
%\todo{What the results show.}

% outer loop + inner loop
\origclearpage
\section{Outer Loop (Divider Approach)}
\label{chap:eval-outer-divider}

We test the \textit{outer loop} in the \textit{divider} island mode using an inventory containing 128 NVIDIA \texttt{DGX-300} and 256 NVIDIA \texttt{H200}.
We select this single inventory due to the diverse nature of the \texttt{DGX-300} and \texttt{H200}, given the \texttt{DGX-300} has approximately three times the compute.
This inventory allows us to execute experiments on a strongly heterogeneous cluster.
The goal of these experiments is to confirm parameter choices for the Bayesian Optimisation loop.

\paragraph{Experiment Setup}
As the \textit{outer loop} is non deterministic, we execute the experiment several times and take averages of the results (trials).
The number of trials is specifically shown under every figure, where we balance accuracy versus compute time.

\paragraph{Plot Description}
Each \textit{outer loop} plot contains a line of mean best throughput, this is the mean throughput across the best solutions in each trial, calculated at a specific iteration number.
Plots also contain min-max ranges and standard deviations, this is calculated similarly to mean throughput; values are computed at each specific iteration index across all trials at that point.
Finally, a line of maximum throughput is also plotted, this is the maximum throughput across all trails, iterations and experiments.
\autoref{fig:05-batch-analysis} contains a reference of this style of plot.

% Batch Size
\subsection{Batch Size Analysis}

\begin{quote}
``How does changing the batch size, of solutions tested at each iteration, affect the results of the Bayesian Optimisation outer loop?''
\end{quote}

We test the throughput performance of the scheduler using different batch size parameter values for the Bayesian Optimisation model.
This increases the number of solutions proposed at each iteration of the acquisition function.
Increasing the batch size has several benefits, specifically higher quality solutions with less variance.
However this comes at the drawback of extra compute time for every iteration cycle.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{outer_batch_error_area.pdf}
    \caption[Throughput per iteration number for different batch sizes]
    {Throughput per iteration number for different batch sizes. \textbf{Experiment Parameters:} iterations = $20$, trials = $10$, min island size = $2$, skew range = $\pm 5.0$.}
    \label{fig:05-batch-analysis}
\end{figure}

Displayed in \autoref{fig:05-batch-analysis}, as the batch size increases, the variance of solutions decreases.
This is expected behaviour as if we increase the number of solutions proposed, it is more likely one of these solutions is good.
Shown in \autoref{fig:05-batch-deltas}, this does however result in increased execution time, where execution time is approximately proportional to batch size.
Execution time is defined as the complete time from start to finish of all iterations, measured per trial.
We select batch size 16 for the final scheduler, but acknowledge this comes at the cost of requiring more compute time to complete.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.4\textwidth]{outer_batch_deltas.pdf}
    \caption[Execution time of scheduler for different batch sizes]
    {Execution time of scheduler for different batch sizes. Using experiment parameters as of \autoref{fig:05-batch-analysis}}
    \label{fig:05-batch-deltas}
\end{figure}

% Skew
\subsection{Skew Range Analysis}

\begin{quote}
``How does changing the range of the skew search parameter affect the results of the outer loop whilst using the divider approach?''
\end{quote}

Next we test how altering the skew search parameter affects the scheduling throughput value.
Displayed in \autoref{fig:05-skew-analysis}, higher skew ranges result in marginally higher throughputs, whilst retaining similar variance to smaller ranges.

This result is logical as we are able to traverse more of the island division search-space when the range is higher, unlocking possibly better solutions.
Despite the increased search space, this completes in approximately the same execution time.
This is expected, as we are not altering the parameters of the Bayesian Optimisation execution itself, but rather the parameters it uses to search. 
Hence, we use the full $\pm5.0$ skew range in the main scheduler due to its improved solution generation.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{outer_skew_error_area.pdf}
    \caption[Throughput per iteration number for different skew ranges (Divider)]
    {Throughput per iteration number for different skew ranges (Divider). \textbf{Experiment Parameters:} batch size = $8$, iterations = $20$, trials = $10$, minimum island size = $2$}
    \label{fig:05-skew-analysis}
\end{figure}

% Islands
\origclearpage
\subsection{Minimum Island Size Analysis}
\label{chap:min-size}

\begin{quote}
``How does changing the minimum number of GPUs in an island affect the results of the in outer loop whilst using the divider approach?''
\end{quote}

We also test how altering the minimum island size affects the result of the optimisation.
Like the skew value analysis, this is altering a search parameter, not an execution parameter of the Bayesian Optimisation.

\autoref{fig:05-min-island-analysis} displays that for every increase in minimum island size, effective throughput halves.
This is due to the prefill phase preferring more smaller islands, than fewer bigger islands, and by increasing the minimum size, fewer small islands are generated.
We select the minimum number of islands to be two for the final scheduler.
While we would likely see performance gains with a minimum size of one, this parameter acknowledges that GPU allocation may be restricted to pairs of GPUs in cloud environments.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{outer_island_error_area.pdf}
    \caption[Throughput per iteration number for different minimum island sizes (Divider)]
    {Throughput per iteration number for different minimum island sizes (Divider). \textbf{Experiment Parameters:} batch size = $8$, iterations = $20$, trials = $10$, skew range = $\pm 5.0$.}
    \label{fig:05-min-island-analysis}
\end{figure}

%\subsection{Summary}
%
%\todo{What have we learnt.}

% outer loop + inner loop
\section{Outer Loop (Direct Approach)}

We also test the \textit{outer loop} in \textit{direct} mode, to demonstrate the performance relative to the \textit{divider} island generation approach.
These experiments use the exact same experiment setup, input inventory and workload trace as of \autoref{chap:eval-outer-divider}.
We use these experiments to analyse the difference in performance between the standard BO and SAASBO optimisers.

\subsection{Batch Size Analysis}

\begin{quote}
``How does changing the batch size, of solutions tested at each iteration, affect the results of the outer loop whilst using the direct approach?''
\end{quote}

We first test using the SAASBO Bayesian Optimisation approach.
\autoref{fig:05-direct-saasbo-batch-analysis} displays that for every increase in batch size, throughput also improves, at the cost of higher variance.
Comparing against the \textit{direct} island generation performance, this approach performs significantly worse, with maximum throughput values of just 300 requests/s.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{outer_old_saasbo_error_area.pdf}
    \caption[Throughput per iteration number for different batch sizes (Direct SAASBO)]
    {Throughput per iteration number for different batch sizes (Direct SAASBO). \textbf{Experiment Parameters:} iterations = $10$, trials = $5$, k slots = $16$}
    \label{fig:05-direct-saasbo-batch-analysis}
\end{figure}

We also complete the same analysis of the \textit{direct} island generation approach whilst using standard Bayesian Optimisation.
Surprisingly \autoref{fig:05-direct-bo-batch-analysis} performs better (faster convergence) than the SAASBO approach of \autoref{fig:05-direct-saasbo-batch-analysis}.
Furthermore the BO approach executes significantly faster than the SAASBO, \autoref{fig:05-batch-deltas-comparison}.
Whilst the \textit{direct} generation approach is categorically worse, it is interesting to observe that SAASBO is not necessarily required on such high dimensionality optimisation.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{outer_old_bo_error_area.pdf}
    \caption[Throughput per iteration number for different batch sizes (Direct BO)]
    {Throughput per iteration number for different batch sizes (Direct BO). \textbf{Experiment Parameters:} iterations = $10$, trials = $5$, k slots = $16$}
    \label{fig:05-direct-bo-batch-analysis}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.85\textwidth]{outer_batch_deltas_comparison.pdf}
    \caption[Execution time of direct scheduler (BO vs SAASBO) for different batch sizes]
    {Execution time of direct scheduler (BO vs SAASBO) for different batch sizes. Using experiment parameters as of \autoref{fig:05-direct-bo-batch-analysis} and \autoref{fig:05-direct-saasbo-batch-analysis}}
    \label{fig:05-batch-deltas-comparison}
\end{figure}


\origclearpage
\subsection{K Slots Analysis}

\begin{quote}
``How does changing K slots value, of solutions tested at each iteration, affect the results of the outer loop whilst using the direct approach?''
\end{quote}

We perform one last experiment to try understand the behaviour of the \textit{divider} island generation approach.
Specifically, we adjust the k slots variable (variable responsible for the high dimensionality of this approach).

Displayed in \autoref{fig:05-direct-saasbo-kslots-analysis}, as k slots increases so does performance, at the cost of significantly higher variance.
As demonstrated in \autoref{chap:min-size} more small islands are better for prefill performance.
Hence, increasing k slots improves performance, but with the downside of exploding dimensionality.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{outer_old_saasbo_kslots_error_area.pdf}
    \caption[Throughput per iteration number for different K slots (Direct SAASBO)]
    {Throughput per iteration number for different K slots (Direct SAASBO). \textbf{Experiment Parameters:} batch size = $4$, iterations = $10$, trials = $5$}
    \label{fig:05-direct-saasbo-kslots-analysis}
\end{figure}

% Introduce where the traces have come from
\section{Workload Traces}
\graphicspath{{chapters/04design/graphs}}
\label{chap:traces}

% The main set of traces (Azure LLM)
\subsubsection{Azure LLM}

We use the Azure LLM \autocite{stojkovicDynamoLLMDesigningLLM2024} workload traces for our experiments with the developed scheduler.
The datasets provide a sample lists of requests in a time period, served by Azure's servers.
We convert these into probability models shown in \autoref{fig:05-traces-azure}, these are used as the input workload to the scheduler.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{traces_azure.pdf}
    \caption[Azure LLM Context and Generated Tokens]
    {Azure LLM Context and Generated Tokens}
    \label{fig:05-traces-azure}
\end{figure}

\section{Discussion}

Within the evaluation section we successfully demonstrated the performance of the scheduling algorithm, comparing heterogenous and homogenous clusters executing the same workload.
The heterogeneous cluster performed with $1.4\times$ higher throughput than the homogenous for code and and $1.2\times$ higher for conversation workloads.

The investigation inside the \textit{inner loop} demonstrated the scheduler is able to precisely map allocation using the expected workload probability model.
The detailed \textit{outer loop} analysis investigated the best search parameters for the Bayesian Optimisation process.
The high skew and low minimum island count produced better throughput results.
The results also displayed a larger batch size significantly improves performance, but increases execution time.

We also demonstrated the \textit{divider} island approach performs significantly better than the \textit{direct} approach.
Even under best conditions for the \textit{direct} method, it performs at over half the throughput compared to the \textit{divider} approach.

